services:
  backend:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - MONGODB_URI=${MONGODB_URI}

  frontend:
    build: ./client
    ports:
      - "5173:5173"
    depends_on:
      - backend
